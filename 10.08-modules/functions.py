# NOTE: It is common practice to put functions that perform similar tasks
#       into their own file, rather than putting all of our code into the same
#       file, so that we're not opening up files that are thousands of lines
#       long and trying to remember where we were.

def f(x):
    x = x + 1
    return x